{
  "type": "object",
  "properties": {
    "blockedUrlPatterns": {
      "title": "Blocked URL patterns",
      "description": "URL patterns where Claw in Chrome is blocked. Patterns match the hostname and path, use '*' as a wildcard, and ignore case, scheme, and a leading 'www.'.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
